---
import ThemeToggle from "./ThemeToggle.astro";
---

<header class="fixed inset-x-0 top-0 z-10 flex justify-center px-4 py-3 text-black">
  <nav
    aria-label="Main navigation"
    class="navbar-shell flex w-full max-w-xl items-center justify-center gap-1 rounded-full px-2 py-2"
  >
    <a class="nav-link" href="/">Home</a>
    <a class="nav-link" href="/posts">Blog</a>
    <span class="nav-divider" aria-hidden="true">|</span>
    <ThemeToggle />
  </nav>
</header>

<style>
  .navbar-shell {
    background: transparent;
    transition:
      background-color 180ms ease,
      box-shadow 180ms ease,
      backdrop-filter 180ms ease;
    animation: nav-shadow 1ms linear both;
    animation-timeline: scroll();
    animation-range: 0 100px;
  }

  .nav-divider {
    color: currentColor;
    font-size: 0.875rem;
    line-height: 1;
    opacity: 0.35;
    user-select: none;
  }

  .nav-link {
    border-radius: 9999px;
    color: currentColor;
    font-size: 0.875rem;
    font-weight: 500;
    padding: 0.5rem 0.8rem;
    transition: background-color 160ms ease, opacity 160ms ease;
  }

  .nav-link:hover,
  .nav-link:focus-visible {
    background: rgb(0 0 0 / 0.06);
    opacity: 0.7;
    outline: none;
  }

  @keyframes nav-shadow {
    to {
      background: rgb(255 255 255 / 0.72);
      box-shadow: 0 8px 24px rgb(0 0 0 / 0.08);
      backdrop-filter: blur(14px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .navbar-shell {
      animation: none;
    }
  }
</style>
